# AI 掌纹趣味分析小程序 — 开发计划

> 创建：2026-08-17 · MVP 周期：约 3 周（业余时间，每天 2-3 小时）
> 定位：**趣味娱乐类**掌纹解读工具（非命理预测）

---

## 0. 一句话定位

用户拍一张手掌照片 → AI 生成一份**性格倾向趣味报告**（事业/感情/财富/习惯四个维度）→ 引导分享裂变。

**核心原则：这是一款"趣味测试"产品，不是"算命"产品。** 所有文案、命名、报告内容都围绕这一点。

---

## 1. 合规红线与过审策略（项目成败的关键）

### 1.1 风险认知

| 风险 | 等级 | 说明 |
|------|------|------|
| 封建迷信类违规 | 🔴 高 | 微信明令禁止算命/占卜/看相类内容，处罚为驳回、下架、封禁 |
| 深度合成/AIGC 类目 | 🟡 中 | AI 问答/生成类功能可能被要求补充深度合成类目+算法备案 |
| 生物特征信息合规 | 🟡 中 | 手掌照片属个人敏感信息，需明示收集目的并最小化存储 |
| 备案主体限制 | 🟢 低 | 个人主体可选类目有限，无"娱乐"类目 |

### 1.2 过审策略（按优先级执行）

1. **命名去玄学化**：小程序名、简介、页面标题均不出现"手相、算命、占卜、大师、运势、命运、风水、吉凶"等词。
   - 定名：「**掌纹测运**」（2026-08-17 微信平台名称查重通过、无风险提示；弃用「掌纹图鉴」（搜索弱），改名顺位预案：「掌纹性格测试」→「掌纹测试」→「AI掌纹分析」）
   - 头像定稿：`design/logo.png`（576×576，宣纸底 + 墨线手掌 + 朱砂三主线，无印章；源文件 design/logo.html 可再生）
2. **类目选择**：
   - 个人主体 → 「工具 > 效率」或「生活服务」（提审时话术：基于图像识别的趣味性格分析工具）
   - 若未来注册企业主体 → 可尝试「娱乐 > 趣味互动」
3. **全站免责声明**（三处必放：首页底部、报告页底部、分享海报角落）：
   > 本内容基于传统掌纹文化的趣味解读，仅供娱乐，不构成任何科学依据或决策建议。
4. **报告内容红线**（写进 System Prompt 强约束）：
   - ❌ 禁止：预测寿命/死亡、健康诊断、绝对化断言（"必定""命中注定"）、灾祸/凶险表述
   - ✅ 只做：性格倾向、行为习惯、积极建议，措辞用"倾向于""可能""仅供参考"
5. **图片最小化存储**：手掌照片分析完成后**立即删除**，数据库只存文本报告（同时降低存储成本与隐私风险）。
6. **隐私接口声明**：`app.json` 配置隐私协议弹窗（相机/相册权限用途明示），后台同步填写《小程序用户隐私保护指引》。
7. **提审物料**：提审页面放"娱乐性质说明"截图，版本备注主动说明"趣味测试工具，非迷信内容"。
8. **「测运」名称风险对冲**（名称带运、内容零运）：
   - 简介与全站 UI/报告文案禁用：运势、运气、好运、转运、旺、桃花劫——「测运」二字仅出现在品牌名
   - 提审备注主动解释：「测运」为产品昵称，实际功能为基于图像识别的趣味性格倾向分析
   - 改名预案：若提审要求改名 → 立即换「掌纹性格测试」（查重顺位见上），仅损失一次提审周期
   - `validate.ts` 关键词黑名单同步加入上述"运"系词汇

### 1.3 Plan B（连续两次被驳回时启用）

- 公众号 H5 版本（无小程序类目审核，域名备案即可）
- 抖音小程序（对趣味测试类相对宽松）

---

## 2. 技术架构

```
微信小程序（原生 + TypeScript）
    │  wx.chooseMedia 拍照/相册
    │  wx.cloud.uploadFile 上传手掌图
    ▼
微信云开发
    ├── 云函数 analyze（Node.js 18）
    │     ├── 每日次数校验（防刷）
    │     ├── 下载图片 → base64
    │     ├── 调用智谱 GLM-4V-Flash（免费）← API Key 只存云函数环境变量
    │     ├── 解析/校验模型 JSON 输出（schema 校验，失败重试 1 次）
    │     ├── 写入 analyses 集合
    │     └── 删除云存储图片
    ├── 云数据库：users / analyses
    └── 流量主激励视频广告（UV≥1000 后接入）
```

**选型理由**：免运维、免域名备案、免 HTTPS 配置，API Key 不出服务端，个人开发者 MVP 最快路径。

---

## 3. MVP 功能清单

### P0（首版必须）
- [ ] 手掌拍照/上传页：轮廓引导框（手掌放置示意）、光线提示
- [ ] 图片质检前置提示：过暗/模糊给用户提示后仍允许提交（质检交给模型判断）
- [ ] AI 分析页：加载动画（3-5 个趣味掌纹知识轮播，掩盖 8-15s 等待）
- [ ] 报告页：总评 + 性格/事业/感情/财富四维卡片 + 综合趣味评分 + 免责声明
- [ ] 每日免费次数限制（3 次/天，纯防刷，不收费）
- [ ] 历史记录页（只存文本报告）
- [ ] 分享卡片（onShareAppMessage 基础版）

### P1（过审后第二版）
- [ ] 激励视频广告：看广告额外 +1 次（前提：流量主开通，UV≥1000）
- [ ] Canvas 生成分享海报（含报告摘要 + 小程序码）
- [ ] 双手对比分析（左手天赋/右手发展 的话术包装）

### P2（有数据后再决定）
- [ ] 面相/面部分析（复用整套体系，需重新评估合规）
- [ ] 报告 H5 化用于朋友圈分享

---

## 4. 页面与路由

```
pages/index      首页：品牌头图 + 拍照入口 + 今日剩余次数 + 免责声明
pages/capture    拍摄页：相机/相册 + 手掌引导框（左右手切换实时镜像，引导图为镜头视角）
pages/analyzing  分析中：用户照片+扫描线 + 轮播文案（云函数真实调用，未部署自动降级 mock）
pages/report     报告页：图鉴类型徽章 + 三主线 + 四维 + 场景速读 + 分享/海报
pages/history    历史列表（本地存储，Phase 2 接云端）
pages/collection 图鉴收集页：12 型网格 + 解锁判定 + 详情弹层
pages/about      关于页：免责声明全文 + 隐私说明
```

---

## 5. 数据库设计

```js
// users
{
  _id, _openid,
  dailyCount: 0,        // 今日已用次数
  lastUsedDate: '2026-08-17',
  createdAt, updatedAt
}

// analyses（只存文本，不存图）
{
  _id, _openid,
  hand: 'right',        // left / right
  result: {             // 模型输出，schema 校验后落库
    summary, personality: [], career, love, wealth, funScore, advice: []
  },
  modelVersion: 'glm-4.6v-flash',
  createdAt
}
```

---

## 6. AI Prompt 设计（产品核心资产）

**System Prompt 要点**：
1. 角色设定：趣味掌纹文化解读员，娱乐向、口吻轻松专业
2. 输入：手掌照片 + 用户性别/年龄段（可选填，提升代入感）
3. 硬约束（合规三条）：不做健康/寿命论断；不用绝对化措辞；不用迷信词汇
4. 输出：严格 JSON schema（summary / personality / career / love / wealth / funScore / advice）
5. 基础掌纹知识注入：三大主线（生命线≈活力状态、智慧线≈思维风格、感情线≈情感表达）+ 常见纹路特征描述，保证输出稳定有据

**工程细节**：
- JSON 解析失败 → 降级重试 1 次 → 仍失败返回兜底文案（不可白屏）
- 云函数超时设 60s；模型超时 30s
- 后续 A/B：GLM-4V-Flash（免费）效果若不稳，小流量切 GLM-4V-Plus 对比（单次成本约 1-2 分钱）

---

## 7. 项目结构（遵循小文件、高内聚原则）

```
zhangwentujian/
├── design/                        # 设计定稿：preview.html（5 图版界面）+ hand-paths.json（掌纹 SVG 路径/命名档案）
├── miniprogram/
│   ├── app.ts / app.json / app.wxss
│   ├── config/index.ts            # 常量：每日次数、超时、版本
│   ├── data/palm-types.ts         # 掌纹人格图鉴 12 型数据（产品核心资产）
│   ├── types/index.ts             # 数据模型（报告/记录/场景）
│   ├── utils/
│   │   ├── request.ts             # 云函数调用封装（统一错误处理）
│   │   ├── quota.ts               # 次数计算（纯函数，可单测）
│   │   ├── format.ts              # 报告数据格式化（纯函数，可单测）
│   │   ├── classify.ts            # 三线数值→图鉴类型（确定性纯函数）
│   │   ├── share.ts               # 全站分享文案（类型+稀有度钩子）
│   │   ├── poster.ts              # Canvas 海报绘制（宣纸图鉴风）
│   │   └── mock-report.ts         # 云端不可用时的本地降级报告
│   └── pages/...
├── cloudfunctions/
│   └── analyze/
│       ├── index.ts               # 入口编排
│       ├── zhipu.ts               # 智谱 API 客户端（独立，可单测）
│       ├── prompt.ts              # System Prompt 常量
│       ├── validate.ts            # 报告 JSON schema 校验
│       └── quota.ts               # 每日限额逻辑
├── tests/                         # jest 单测（utils + 云函数，mock 智谱）
├── e2e/                           # miniprogram-automator 冒烟测试
└── PLAN.md
```

---

## 8. 里程碑（3 周 + 1 周缓冲）

### Phase 0 · 准备（8/17-8/19）
- [x] 注册小程序（个人主体），选「工具>效率」类目 → **AppID: wx3b8baf398bf449d2**（2026-08-17，名称「掌纹测运」通过，头像 design/logo.png）
- [x] 注册智谱开放平台，拿 API Key，跑通 GLM-4.6V-Flash → 真实端到端验证完成（commit a58d33c，含 thinking 关闭 + 429 退避）
- [x] 开通云开发环境，云函数环境变量注入 API Key（云函数编排 + 云存储上传已打通，commit d872d59）
- [ ] **用 20 张真实手掌图人工评估模型输出质量**——不过关就换模型，别写代码

### Phase 1 · 骨架（8/20-8/24）→ 提前完成（8/17）
- [x] 项目初始化（TS + 目录结构 + 统一 request 封装）；typecheck 双端零错误，jest 26/26 通过
- [x] 6 个页面静态布局 + 路由跳转（index/capture/analyzing/report/history/about，图鉴风 1:1 还原；report 用 mock 数据）→ **待微信开发者工具真机预览确认视觉**
- [x] 视觉风格：宣纸/墨/朱砂「图鉴」风（2026-08-17 定稿，见 design/preview.html——衬线标题、印章评分、界格线；朱砂色仅用于主线/印章/CTA。原「星空紫/暗金」方案作废）

### Phase 1.5 · 体验与传播层（8/17-8/18 提前交付，原计划外增量）
- [x] **掌纹人格图鉴 12 型体系**（对标 MBTI 传播机制）：`data/palm-types.ts`（12 型：编号/命名/趣味稀有度/tagline/相性/印章字）+ `utils/classify.ts`（三线数值→类型的本地确定性纯函数，**模型不参与分类**，集合封闭可控，43 项单测锁边界）
- [x] 图鉴收集页：历史记录推导解锁、12 型网格（未解锁显示稀有度钓鱼）、详情弹层、收集进度分享文案
- [x] 报告场景速读：工作/生活/身心三场景（特质+注意事项）；身心维度仅生活方式层（合规硬边界写进 prompt）
- [x] 全页面统一分享（utils/share.ts：类型+稀有度钩子文案）+ Canvas 分享海报（宣纸图鉴风，类型名上墙，可存相册）
- [x] 前端数据链路本地闭环：真图进分析页 → 云函数调用（未部署自动降级 mock）→ 报告落本地库（上限20条）→ 历史真记录 → 配额真实消耗
- [x] HTML 交互预览版（design/app-preview.html，7 屏手机壳，样式 1:1 翻译）

### Phase 2 · 核心链路（8/25-8/30）→ 提前完成（8/21，待真机回归收尾）
- [x] 拍照/上传 → 云存储 → 云函数 → 智谱 → 报告落库 全链路
- [x] 每日限额 + 防刷（openid 维度；云端权威 + 本地镜像 + 分享奖励云端限次。防刷超出配额部分经决策 v1 不做，见 openspec/changes/phase-2-closeout）
- [x] 报告页渲染 + JSON 校验降级（校验失败重试 1 次 → 兜底：不扣配额、横幅提示重拍、不进历史/图鉴）
- [x] 单测：quota / validate / zhipu mock 测试（59 passed，真实模型 E2E 无 Key 自动跳过）

### Phase 3 · 完善与提审（8/31-9/6）
- [ ] 历史记录、分享卡片、免责声明三处布置
- [ ] 隐私协议配置 + 后台隐私指引填写
- [ ] E2E 冒烟（automator：拍照→报告 全流程）
- [ ] 体验版真机测试（iOS/Android 各 1 台）
- [ ] **提审**（备好娱乐性质说明话术）

### Phase 4 · 过审迭代（9/7-9/13，视审核情况）
- [ ] 处理驳回意见 / 上线后 bug 修复
- [ ] UV≥1000 后接激励视频广告
- [ ] 数据埋点：完成率、分享率、次留

---

## 9. 成本与收益测算

| 项目 | 成本 |
|------|------|
| 小程序注册（个人主体） | 0 元 |
| 云开发基础套餐 | 约 19.9 元/月 |
| GLM-4V-Flash | 0 元（免费模型） |
| **月固定成本** | **约 20 元/月** |

收益预期（务实估计）：激励视频 eCPM 娱乐类约 5-20 元/千次；需先达到 UV 1000 开通流量主。**前 1-2 个月目标定为跑通流程和积累用户，不指望覆盖成本。**

---

## 10. 测试计划

- **单元测试**（jest）：quota 计算、JSON schema 校验、报告格式化、智谱客户端（mock HTTP）
- **E2E 冒烟**（miniprogram-automator）：进入→拍照（mock）→报告展示→历史可见
- **模型质量人工评估**：20 张样图 × 评分表（内容合理度/合规度/趣味度），Phase 0 完成准入
- **真机回归**：每次提审前 iOS + Android 各过一遍核心链路

---

## 11. 风险清单

| 风险 | 概率 | 应对 |
|------|------|------|
| 审核驳回（迷信类） | 高 | §1.2 全套策略 + Plan B（H5） |
| 模型输出不稳定/质量差 | 中 | Phase 0 人工准入 + 切 GLM-4V-Plus + prompt 迭代 |
| 免费模型被滥用刷量 | 中 | 每日限额 + 云函数频控 + 异常 openid 拉黑 |
| 报告含违规表述 | 中 | Prompt 硬约束 + validate.ts 关键词黑名单二次过滤 |
| UV 达不到 1000 | 中 | 分享裂变海报（P1）优先级提高 |

---

## 12. 上线后迭代方向

1. 分享海报美化（裂变核心）
2. 双手对比话术版
3. 用户反馈入口（收集吐槽，反哺 prompt）
4. 数据看板：漏斗（进入→拍摄→完成→分享）
