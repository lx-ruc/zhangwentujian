{
  "name": "zhangwentujian",
  "version": "0.1.0",
  "private": true,
  "description": "掌纹测运 — AI 掌纹趣味测试微信小程序",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json && tsc --noEmit -p cloudfunctions/tsconfig.json",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^18.19.0",
    "jest": "^29.7.0",
    "miniprogram-api-typings": "^3.12.0",
    "ts-jest": "^29.1.4",
    "typescript": "^5.4.5"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": ["<rootDir>/tests/**/*.test.ts"]
  }
}
