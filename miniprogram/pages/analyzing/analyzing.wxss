.page-analyzing {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 64rpx 56rpx calc(48rpx + env(safe-area-inset-bottom));
  box-sizing: border-box;
}

/* ===== 头部 ===== */
.head { text-align: center; }
.title {
  font-family: var(--font-display);
  font-weight: 900;
  font-size: 44rpx;
  letter-spacing: 4rpx;
}
.eta {
  display: block;
  margin-top: 10rpx;
  font-family: var(--font-mono);
  font-size: 20rpx;
  letter-spacing: 3rpx;
  color: var(--ink-2);
}
.progress {
  margin-top: 28rpx;
  width: 100%;
  height: 6rpx;
  background: var(--paper-2);
  border-radius: 3rpx;
  overflow: hidden;
}
.bar {
  height: 100%;
  background: var(--cinnabar);
  border-radius: 3rpx;
  transition: width 0.4s linear;
}

/* ===== 手掌 ===== */
.plate { margin-top: 36rpx; }
.hand {
  width: 380rpx;
  height: 400rpx;
  animation: breathe 2.2s ease-in-out infinite;
}
.hand.settled { animation: none; }
@keyframes breathe {
  0%, 100% { transform: scale(1); opacity: 0.92; }
  50% { transform: scale(1.03); opacity: 1; }
}

/* ===== 趣味知识 ===== */
.fact-card {
  margin-top: 44rpx;
  width: 100%;
  background: var(--paper-2);
  border: 1rpx solid var(--rule);
  border-radius: 16rpx;
  padding: 32rpx 36rpx;
  box-sizing: border-box;
}
.fact-tag {
  font-family: var(--font-mono);
  font-size: 20rpx;
  letter-spacing: 2rpx;
  color: var(--cinnabar);
}
.fact-text {
  display: block;
  margin-top: 16rpx;
  min-height: 120rpx;
  font-size: 27rpx;
  line-height: 1.8;
  color: var(--ink);
}
.dots {
  margin-top: 20rpx;
  display: flex;
  justify-content: center;
  gap: 12rpx;
}
.dot {
  width: 10rpx; height: 10rpx;
  border-radius: 50%;
  background: var(--ink-3);
  opacity: 0.4;
}
.dot.on { background: var(--cinnabar); opacity: 1; }
