<view class="page-history" style="padding-top:{{navTop}}px">
  <view class="head">
    <text class="title">解读记录</text>
    <text class="privacy-chip">照片即焚 · 仅存文字</text>
  </view>

  <block wx:if="{{records.length}}">
    <view class="record card" wx:for="{{records}}" wx:key="id" bindtap="openRecord" data-id="{{item.id}}">
      <view class="record-head">
        <text class="date">{{item.date}}</text>
        <text class="hand">{{item.hand}}</text>
        <view class="mini-seal">{{item.score}}</view>
      </view>
      <text class="digest">{{item.digest}}</text>
      <view class="tags">
        <text class="tag" wx:for="{{item.tags}}" wx:for-item="tag" wx:key="*this">{{tag}}</text>
      </view>
    </view>
  </block>

  <view wx:else class="empty">
    <image class="empty-hand" src="/assets/hand-plate.png" mode="aspectFit" />
    <text>还没有解读记录</text>
    <text class="empty-sub">拍一张手掌照片，开始你的第一份趣味报告</text>
    <button class="cta" bindtap="goCapture">去拍摄</button>
  </view>

  <text class="disclaimer">{{disclaimer}}</text>
</view>
